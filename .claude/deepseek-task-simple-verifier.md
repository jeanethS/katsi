# Task: Verifier Testing

Create a pytest test file `tests/test_verifier_scenarios.py` with tests for:

1. Verifier success (exit code 0)
2. Verifier failure (exit code != 0)  
3. Verifier timeout (exceeds time limit)
4. Owner approval after verifier success
5. Owner rejection despite verifier success

Focus on realistic scenarios with mock verifiers. Keep it simple and practical.